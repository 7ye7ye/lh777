# 患者身份认证模块测试用例

## 1. 后端接口测试

### 1.1 患者身份认证申请接口测试

#### 测试用例 1.1.1：正常提交身份认证申请
- **请求方法**：POST
- **请求URL**：`/patient/identity/apply`
- **请求体**：
```json
{
  "patientId": 1,
  "identityPhoto": "http://example.com/photos/identity.jpg",
  "handheldIdentityPhoto": "http://example.com/photos/handheld.jpg",
  "studentId": "20210101"
}
```
- **预期结果**：
  - 状态码：200
  - 返回消息：身份认证申请已提交，请等待管理员审核
  - 数据库中生成一条状态为0（待审核）的认证记录
  - 数据库中生成一条操作类型为"SUBMIT"的审核日志

#### 测试用例 1.1.2：提交身份认证申请但缺少必要字段
- **请求方法**：POST
- **请求URL**：`/patient/identity/apply`
- **请求体**：
```json
{
  "patientId": 1,
  "identityPhoto": "http://example.com/photos/identity.jpg"
}
```
- **预期结果**：
  - 状态码：400
  - 返回消息：请先上传手持证件照片

#### 测试用例 1.1.3：提交身份认证申请但图片格式不正确
- **请求方法**：POST
- **请求URL**：`/patient/identity/apply`
- **请求体**：
```json
{
  "patientId": 1,
  "identityPhoto": "http://example.com/photos/identity.gif",
  "handheldIdentityPhoto": "http://example.com/photos/handheld.jpg",
  "studentId": "20210101"
}
```
- **预期结果**：
  - 状态码：400
  - 返回消息：证件照片格式不正确，请上传JPG或PNG格式的图片

#### 测试用例 1.1.4：重新提交已被驳回的身份认证申请
- **前置条件**：患者ID为1的身份认证状态为2（已驳回）
- **请求方法**：POST
- **请求URL**：`/patient/identity/apply`
- **请求体**：
```json
{
  "patientId": 1,
  "identityPhoto": "http://example.com/photos/identity_new.jpg",
  "handheldIdentityPhoto": "http://example.com/photos/handheld_new.jpg",
  "studentId": "20210101"
}
```
- **预期结果**：
  - 状态码：200
  - 返回消息：身份认证申请已提交，请等待管理员审核
  - 数据库中更新认证记录状态为0（待审核）
  - 数据库中生成一条操作类型为"RESUBMIT"的审核日志

### 1.2 管理员审核身份认证接口测试

#### 测试用例 1.2.1：管理员通过身份认证申请
- **请求方法**：POST
- **请求URL**：`/patient/identity/approve`
- **请求体**：
```json
{
  "patientId": 1,
  "approve": true,
  "operatorId": 100,
  "operatorName": "管理员A"
}
```
- **预期结果**：
  - 状态码：200
  - 返回消息：已通过该身份认证申请
  - 数据库中更新认证记录状态为1（已通过）
  - 数据库中生成一条操作类型为"APPROVE"的审核日志

#### 测试用例 1.2.2：管理员驳回身份认证申请但未提供驳回原因
- **请求方法**：POST
- **请求URL**：`/patient/identity/approve`
- **请求体**：
```json
{
  "patientId": 1,
  "approve": false,
  "operatorId": 100,
  "operatorName": "管理员A"
}
```
- **预期结果**：
  - 状态码：400
  - 返回消息：驳回时必须提供驳回原因

#### 测试用例 1.2.3：管理员驳回身份认证申请并提供驳回原因
- **请求方法**：POST
- **请求URL**：`/patient/identity/approve`
- **请求体**：
```json
{
  "patientId": 1,
  "approve": false,
  "rejectReason": "证件照片模糊，无法辨认",
  "operatorId": 100,
  "operatorName": "管理员A"
}
```
- **预期结果**：
  - 状态码：200
  - 返回消息：已驳回该身份认证申请
  - 数据库中更新认证记录状态为2（已驳回）
  - 数据库中记录驳回原因
  - 数据库中生成一条操作类型为"REJECT"的审核日志

### 1.3 获取驳回原因接口测试

#### 测试用例 1.3.1：获取存在的驳回原因
- **前置条件**：患者ID为1的身份认证状态为2（已驳回）
- **请求方法**：GET
- **请求URL**：`/patient/identity/rejectReason/1`
- **预期结果**：
  - 状态码：200
  - 返回数据中包含驳回原因

#### 测试用例 1.3.2：获取不存在的患者的驳回原因
- **请求方法**：GET
- **请求URL**：`/patient/identity/rejectReason/999`
- **预期结果**：
  - 状态码：404
  - 返回消息：未找到对应就诊卡

### 1.4 获取审核历史接口测试

#### 测试用例 1.4.1：获取存在审核历史的患者记录
- **请求方法**：GET
- **请求URL**：`/patient/identity/auditHistory/1`
- **预期结果**：
  - 状态码：200
  - 返回数据中包含审核历史记录列表
  - 列表按时间倒序排列

#### 测试用例 1.4.2：获取不存在的患者的审核历史
- **请求方法**：GET
- **请求URL**：`/patient/identity/auditHistory/999`
- **预期结果**：
  - 状态码：404
  - 返回消息：未找到对应就诊卡

## 2. 前端功能测试

### 2.1 患者身份认证提交页面测试

#### 测试用例 2.1.1：表单验证功能
- **操作步骤**：
  1. 访问患者身份认证页面
  2. 不上传任何照片，点击提交按钮
- **预期结果**：
  - 显示错误提示：请上传证件照片
  - 表单不提交

#### 测试用例 2.1.2：图片格式验证
- **操作步骤**：
  1. 访问患者身份认证页面
  2. 尝试上传GIF格式的图片
- **预期结果**：
  - 显示错误提示：只能上传JPG或PNG格式的图片
  - 图片不被接受

#### 测试用例 2.1.3：图片大小验证
- **操作步骤**：
  1. 访问患者身份认证页面
  2. 尝试上传大于5MB的图片
- **预期结果**：
  - 显示错误提示：图片大小不能超过5MB
  - 图片不被接受

#### 测试用例 2.1.4：正常提交认证申请
- **操作步骤**：
  1. 访问患者身份认证页面
  2. 上传合规的证件照片和手持证件照片
  3. 填写必要信息
  4. 点击提交按钮
- **预期结果**：
  - 显示成功提示：身份认证申请已提交，请等待管理员审核
  - 页面状态更新为"审核中"

#### 测试用例 2.1.5：显示驳回状态和原因
- **前置条件**：患者身份认证已被驳回
- **操作步骤**：
  1. 访问患者身份认证页面
- **预期结果**：
  - 显示驳回状态和原因
  - 允许重新提交

### 2.2 管理员审核页面测试

#### 测试用例 2.2.1：筛选功能
- **操作步骤**：
  1. 访问管理员审核页面
  2. 选择状态筛选条件（未审核/已通过/未通过）
  3. 点击查询按钮
- **预期结果**：
  - 列表显示符合筛选条件的记录

#### 测试用例 2.2.2：通过认证申请
- **操作步骤**：
  1. 访问管理员审核页面
  2. 找到状态为"未审核"的记录
  3. 点击"通过"按钮
- **预期结果**：
  - 显示成功提示：已通过该申请
  - 记录状态更新为"已通过"

#### 测试用例 2.2.3：驳回认证申请但未填写原因
- **操作步骤**：
  1. 访问管理员审核页面
  2. 找到状态为"未审核"的记录
  3. 点击"驳回"按钮
  4. 不填写驳回原因，点击确定
- **预期结果**：
  - 显示错误提示：请输入驳回原因
  - 驳回操作不执行

#### 测试用例 2.2.4：驳回认证申请并填写原因
- **操作步骤**：
  1. 访问管理员审核页面
  2. 找到状态为"未审核"的记录
  3. 点击"驳回"按钮
  4. 填写驳回原因，点击确定
- **预期结果**：
  - 显示成功提示：已驳回该申请
  - 记录状态更新为"已驳回"

#### 测试用例 2.2.5：查看驳回原因
- **前置条件**：存在已驳回的认证申请
- **操作步骤**：
  1. 访问管理员审核页面
  2. 找到状态为"已驳回"的记录
  3. 鼠标悬停在驳回原因图标上
- **预期结果**：
  - 显示驳回原因的提示框

### 2.3 审核历史页面测试

#### 测试用例 2.3.1：查看审核历史
- **操作步骤**：
  1. 访问审核历史页面，传入患者ID
- **预期结果**：
  - 以时间线形式显示该患者的所有审核历史
  - 不同操作类型显示不同的图标和颜色

#### 测试用例 2.3.2：查看空审核历史
- **前置条件**：患者没有任何审核历史
- **操作步骤**：
  1. 访问审核历史页面，传入患者ID
- **预期结果**：
  - 显示"暂无审核历史记录"的提示

## 3. 数据库完整性测试

### 测试用例 3.1：验证审核日志记录完整性
- **操作步骤**：
  1. 执行完整的身份认证流程：提交申请 -> 驳回 -> 重新提交 -> 通过
  2. 查询verification_audit_log表中该患者的记录
- **预期结果**：
  - 存在4条记录，分别对应SUBMIT、REJECT、RESUBMIT、APPROVE操作
  - 每条记录包含正确的患者ID、操作人信息、操作描述和时间

### 测试用例 3.2：验证患者表与认证记录表的一致性
- **操作步骤**：
  1. 查询patient表中某患者的identityVerify字段
  2. 查询patient_identity_verify表中该患者的status字段
- **预期结果**：
  - 两个字段的值一致，表示状态同步正确

## 4. 边界条件和异常处理测试

### 测试用例 4.1：处理非法身份证号
- **操作步骤**：
  1. 尝试提交身份认证申请，使用明显错误的身份证号
- **预期结果**：
  - 后端验证失败，返回错误消息：身份证号格式不正确

### 测试用例 4.2：处理特殊字符姓名
- **操作步骤**：
  1. 尝试提交身份认证申请，姓名中包含特殊字符如<>'"等
- **预期结果**：
  - 后端验证失败，返回错误消息：姓名格式不正确

### 测试用例 4.3：处理超大图片
- **操作步骤**：
  1. 尝试上传超过5MB的图片
- **预期结果**：
  - 前端验证失败，显示错误提示：图片大小不能超过5MB

### 测试用例 4.4：处理重复审核
- **前置条件**：患者身份认证已通过
- **操作步骤**：
  1. 尝试再次通过该患者的身份认证
- **预期结果**：
  - 后端验证失败，返回错误消息：该身份认证已经通过，无需重复审核

## 5. 安全性测试

### 测试用例 5.1：验证权限控制
- **操作步骤**：
  1. 使用非管理员账号尝试访问审核接口
- **预期结果**：
  - 访问被拒绝，返回权限不足的错误

### 测试用例 5.2：验证跨用户操作限制
- **操作步骤**：
  1. 使用用户A的账号尝试提交用户B的患者身份认证
- **预期结果**：
  - 操作被拒绝，返回错误消息：无权操作此就诊卡

### 测试用例 5.3：XSS防护测试
- **操作步骤**：
  1. 尝试在驳回原因中注入JavaScript代码
- **预期结果**：
  - 代码被转义或过滤，不会被执行

## 6. 性能测试

### 测试用例 6.1：批量审核性能
- **操作步骤**：
  1. 准备100条待审核记录
  2. 批量执行审核操作
- **预期结果**：
  - 系统能够在合理时间内完成所有审核
  - 数据库中正确记录所有审核日志

### 测试用例 6.2：并发提交测试
- **操作步骤**：
  1. 模拟10个用户同时提交身份认证申请
- **预期结果**：
  - 所有请求都能被正确处理
  - 不出现数据一致性问题
