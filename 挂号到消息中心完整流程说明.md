# 从挂号成功到消息中心显示消息的完整流程

## 📋 流程概览

```
用户点击支付 → 前端调用挂号API → 后端创建挂号记录 → 创建成功消息 → 用户打开消息中心 → 查询并显示消息
```

---

## 🔄 详细流程步骤

### **第一步：前端发起挂号请求**

#### 涉及文件：
- **前端文件**：`校医院挂号系统/subpkg/hospital/payment.vue`
- **API文件**：`校医院挂号系统/api/registration.ts`

#### 操作流程：
1. 用户在支付页面点击"确认支付"
2. 前端调用挂号API：`POST /applet/registration/create`
3. 请求参数包括：
   - `RegistrationRecord` 对象（包含 `scheduleId`, `typeId` 等）
   - `patientId`（患者ID）
   - `joinWaitingQueue`（是否加入候补队列）

---

### **第二步：后端Controller接收请求**

#### 涉及文件：
- **文件路径**：`back-end/silos-module/hospital-module/src/main/java/org/jeecg/modules/hospital/controller/applet/RegistrationAppletController.java`
- **方法**：`createRegistration()`

#### 操作：
```java
@PostMapping("/create")
public Result<String> createRegistration(
    @RequestBody RegistrationRecord record,
    @RequestParam Long patientId,
    @RequestParam(defaultValue = "false") boolean joinWaitingQueue
) {
    // 调用Service层处理业务逻辑
    return registrationService.createRegistration(record, patientId, joinWaitingQueue);
}
```

---

### **第三步：Service层处理挂号业务逻辑**

#### 涉及文件：
- **文件路径**：`back-end/silos-module/hospital-module/src/main/java/org/jeecg/modules/hospital/service/impl/RegistrationServiceImpl.java`
- **方法**：`createRegistration()`

#### 详细操作步骤：

##### 3.1 数据验证
- 检查 `scheduleId`、`typeId`、`patientId` 是否为空
- 验证排班信息是否存在

##### 3.2 数据库查询（第一次）
```sql
-- 查询排班信息
SELECT * FROM doctor_schedule WHERE schedule_id = #{scheduleId}
```
- **涉及表**：`doctor_schedule`
- **Mapper方法**：`registrationMapper.selectScheduleById()`

##### 3.3 检查重复挂号
```sql
-- 检查是否重复挂号
SELECT COUNT(*) FROM registration_record 
WHERE patient_id = #{patientId} 
AND schedule_id = #{scheduleId} 
AND status != 3
```
- **涉及表**：`registration_record`
- **Mapper方法**：`registrationMapper.checkDuplicateBySchedule()`

##### 3.4 查询挂号类型
```sql
-- 查询挂号类型信息
SELECT * FROM registration_type WHERE type_id = #{typeId}
```
- **涉及表**：`registration_type`
- **Mapper方法**：`registrationMapper.selectTypeById()`

##### 3.5 检查号源是否已满
- 比较 `used_quota` 和 `daily_quota`
- 如果已满且未选择候补，返回错误

##### 3.6 查询患者信息
```sql
-- 查询患者信息
SELECT * FROM patient WHERE patient_id = #{patientId}
```
- **涉及表**：`patient`
- **Mapper方法**：`patientMapper.selectById()`

##### 3.7 计算价格
- 根据患者类型（学生/教职工）计算实际价格
- 学生：使用 `student_price`
- 教职工：使用 `staff_price`

##### 3.8 生成挂号单号
- 调用 `generateRegistrationNo()` 生成唯一挂号单号
- 格式：`YYYYMMDD + 科室ID + 序号`

##### 3.9 插入挂号记录（第二次数据库操作）
```sql
INSERT INTO registration_record (
    schedule_id, patient_id, doctor_id, type_id, 
    registration_no, register_time, status, 
    price_original, actual_price, is_add
) VALUES (...)
```
- **涉及表**：`registration_record`
- **Mapper方法**：`registrationMapper.insertRegistration()`
- **关键**：使用 `@Options(useGeneratedKeys = true)` 获取自动生成的 `record_id`

##### 3.10 更新排班已用数量
```sql
UPDATE doctor_schedule 
SET used_quota = #{usedQuota}, update_time = NOW() 
WHERE schedule_id = #{scheduleId}
```
- **涉及表**：`doctor_schedule`
- **Mapper方法**：`registrationMapper.updateScheduleUsedQuota()`

##### 3.11 查询挂号详情（第三次数据库操作）
```sql
SELECT 
    rr.record_id, rr.registration_no, rr.patient_id,
    p.patient_name, p.outpatient_number AS patient_card_no, p.user_id AS patient_user_id,
    d.doctor_name, d.title AS doctor_title,
    dep.dept_name AS department_name, dep.location AS dept_location,
    ds.schedule_date, ds.time_slot,
    rt.type_name,
    COALESCE(rr.price_original, rt.price_original) AS price_original,
    COALESCE(rr.actual_price, rt.price_original) AS actual_price,
    rr.status
FROM registration_record rr
LEFT JOIN patient p ON rr.patient_id = p.patient_id
LEFT JOIN doctor_schedule ds ON rr.schedule_id = ds.schedule_id
LEFT JOIN department dep ON ds.dept_id = dep.dept_id
LEFT JOIN doctor d ON rr.doctor_id = d.doctor_id
LEFT JOIN registration_type rt ON rr.type_id = rt.type_id
WHERE rr.record_id = #{recordId}
```
- **涉及表**：`registration_record`, `patient`, `doctor_schedule`, `department`, `doctor`, `registration_type`
- **Mapper方法**：`registrationMapper.selectRegistrationDetail()`
- **返回对象**：`RegistrationDetailDTO`

##### 3.12 创建成功消息
- 调用 `createSuccessMessage(detail)` 方法

---

### **第四步：创建挂号成功消息**

#### 涉及文件：
- **文件路径**：`back-end/silos-module/hospital-module/src/main/java/org/jeecg/modules/hospital/service/impl/RegistrationServiceImpl.java`
- **方法**：`createSuccessMessage()`

#### 详细操作：

##### 4.1 获取当前登录用户ID
```java
String currentUserId = resolveCurrentUserId();
```
- 从HTTP请求头中获取Token
- 解析Token获取用户名
- 查询 `hos_user` 表获取 `user_id`
- **涉及表**：`hos_user`
- **涉及文件**：`resolveCurrentUserId()` 方法

##### 4.2 创建Message对象
```java
Message message = new Message();
message.setUserId(userId);  // 当前登录用户的ID
message.setAppointmentId(String.valueOf(detail.getRecordId()));  // 挂号记录ID
message.setMessageType("APPOINTMENT_SUCCESS");  // 消息类型
message.setTitle("预约挂号成功提醒");  // 消息标题
message.setCreatedTime(LocalDateTime.now());  // 创建时间
message.setIsRead(false);  // 未读状态
```

##### 4.3 组装消息内容（JSON格式）
```json
{
  "patient_card_no": "P20250001",
  "patient_name": "张三",
  "doctor_name": "李医生",
  "department_name": "内科",
  "appointment_time": "2025-11-21 08:00-12:00",
  "hospital_remark": "请提前15分钟到达诊室候诊"
}
```

##### 4.4 插入消息到数据库（第四次数据库操作）
```sql
INSERT INTO message_log (
    user_id, appointment_id, message_type, 
    title, content, created_time, is_read
) VALUES (...)
```
- **涉及表**：`message_log`
- **Mapper方法**：`messageService.save()`（MyBatis-Plus提供）

##### 4.5 检查是否需要立即创建提醒
- 调用 `appointmentReminderTask.checkAndCreateImmediateReminder(detail)`
- 如果预约日期是明天且当前时间>=8点，立即创建提醒

---

### **第五步：前端打开消息中心**

#### 涉及文件：
- **前端文件**：`校医院挂号系统/pages/messages/messages.vue`
- **生命周期**：`onShow()` 方法

#### 操作：
1. 用户打开消息中心页面
2. 自动触发 `fetchMessageList()` 方法

---

### **第六步：前端请求消息列表**

#### 涉及文件：
- **前端文件**：`校医院挂号系统/pages/messages/messages.vue`
- **方法**：`fetchMessageList()`

#### 操作流程：

##### 6.1 获取当前用户ID
```javascript
let userId = this.getCurrentUserId();
```
- 从 `uni.getStorageSync('userInfo')` 获取用户信息
- 提取 `userId` 字段

##### 6.2 发送HTTP请求
```javascript
GET http://10.61.62.249:8095/jeecg-boot/api/messages/list?userId=262
Headers: {
    'X-Access-Token': uni.getStorageSync('token')
}
```

---

### **第七步：后端Controller接收消息查询请求**

#### 涉及文件：
- **文件路径**：`back-end/silos-module/hospital-module/src/main/java/org/jeecg/modules/hospital/controller/MessageController.java`
- **方法**：`getMessageList()`

#### 操作：
```java
@GetMapping("/list")
public List<Message> getMessageList(@RequestParam String userId) {
    return messageService.listMessagesByUserId(userId);
}
```

---

### **第八步：Service层查询消息（包含提醒检查）**

#### 涉及文件：
- **文件路径**：`back-end/silos-module/hospital-module/src/main/java/org/jeecg/modules/hospital/service/impl/MessageServiceImpl.java`
- **方法**：`listMessagesByUserId()`

#### 详细操作：

##### 8.1 检查并创建缺失的提醒（重要！）
```java
checkAndCreateRemindersForUser(userId);
```

**详细流程**：
1. 检查当前时间是否 >= 8点
2. 计算明天的日期：`LocalDate.now().plusDays(1)`
3. 查询所有明天的预约记录：
   ```sql
   SELECT ... FROM registration_record rr
   LEFT JOIN patient p ON rr.patient_id = p.patient_id
   LEFT JOIN doctor_schedule ds ON rr.schedule_id = ds.schedule_id
   ...
   WHERE ds.schedule_date = #{tomorrow} AND rr.status = 1
   ```
   - **涉及表**：`registration_record`, `patient`, `doctor_schedule`, `department`, `doctor`, `registration_type`
   - **Mapper方法**：`registrationMapper.listRegistrationsByDate()`

4. 通过预约成功消息匹配当前用户：
   ```sql
   SELECT * FROM message_log 
   WHERE appointment_id = #{appointmentId} 
   AND message_type = 'APPOINTMENT_SUCCESS'
   LIMIT 1
   ```
   - **涉及表**：`message_log`
   - 如果成功消息中的 `user_id` 与当前用户匹配，则创建提醒

5. 创建提醒消息（如果还没有）：
   - 调用 `appointmentReminderTask.createReminderForRegistrationWithUserId()`
   - 插入提醒消息到 `message_log` 表
   - 消息类型：`APPOINTMENT_REMINDER`
   - 创建时间固定为当天8点

##### 8.2 查询用户的所有消息（第五次数据库操作）
```sql
SELECT * FROM message_log 
WHERE user_id = #{userId} 
ORDER BY created_time DESC
```
- **涉及表**：`message_log`
- **Mapper方法**：`baseMapper.selectList()`（MyBatis-Plus提供）

---

### **第九步：返回消息列表给前端**

#### 返回数据格式：
```json
[
  {
    "messageId": 1,
    "userId": "262",
    "appointmentId": "23",
    "messageType": "APPOINTMENT_SUCCESS",
    "title": "预约挂号成功提醒",
    "content": "{\"patient_card_no\":\"P20250001\",\"patient_name\":\"张三\",...}",
    "createdTime": "2025-11-19T20:57:55",
    "isRead": false
  },
  {
    "messageId": 2,
    "userId": "262",
    "appointmentId": "23",
    "messageType": "APPOINTMENT_REMINDER",
    "title": "就诊前一天提醒",
    "content": "{...}",
    "createdTime": "2025-11-20T08:00:00",
    "isRead": false
  }
]
```

---

### **第十步：前端渲染消息列表**

#### 涉及文件：
- **前端文件**：`校医院挂号系统/pages/messages/messages.vue`

#### 操作：
1. 接收后端返回的消息列表
2. 解析每条消息的 `content`（JSON字符串转对象）
3. 根据 `messageType` 显示不同的图标和样式
4. 渲染消息卡片到页面

---

## 📊 数据库表关系图

```
hos_user (用户表)
  ├── user_id (主键)
  └── user_account

patient (患者表)
  ├── patient_id (主键)
  ├── user_id (外键 → hos_user.user_id)
  └── patient_name

registration_record (挂号记录表)
  ├── record_id (主键)
  ├── patient_id (外键 → patient.patient_id)
  ├── schedule_id (外键 → doctor_schedule.schedule_id)
  ├── doctor_id (外键 → doctor.doctor_id)
  └── type_id (外键 → registration_type.type_id)

doctor_schedule (排班表)
  ├── schedule_id (主键)
  ├── doctor_id (外键 → doctor.doctor_id)
  ├── dept_id (外键 → department.dept_id)
  └── schedule_date, time_slot

message_log (消息表)
  ├── message_id (主键)
  ├── user_id (外键 → hos_user.user_id)
  ├── appointment_id (关联 → registration_record.record_id)
  └── message_type, title, content, created_time, is_read
```

---

## 🔑 关键点总结

### 1. **用户ID的获取方式**
- **创建消息时**：从Token中解析当前登录用户ID（`hos_user.user_id`）
- **查询消息时**：前端传递的 `userId`（也是 `hos_user.user_id`）
- **匹配预约记录**：通过预约成功消息中的 `user_id` 来匹配

### 2. **消息类型**
- `APPOINTMENT_SUCCESS`：预约挂号成功提醒（创建挂号时立即创建）
- `APPOINTMENT_REMINDER`：就诊前一天提醒（查询消息时检查并创建）
- `APPOINTMENT_ONE_HOUR`：就诊前一小时提醒（定时任务创建）

### 3. **提醒创建时机**
- **立即提醒**：如果预约日期是明天且当前时间>=8点，创建挂号时立即创建
- **查询时创建**：用户打开消息中心时，如果满足条件（明天预约+当前时间>=8点），自动创建提醒
- **定时任务**：每天早上8点执行，为所有明天的预约创建提醒

### 4. **数据库操作次数**
- **创建挂号**：4次数据库操作（查询排班、插入记录、更新排班、查询详情）
- **创建消息**：1次数据库操作（插入消息）
- **查询消息**：2-3次数据库操作（查询预约记录、查询成功消息、插入提醒、查询消息列表）

---

## 📝 涉及的主要文件清单

### 后端文件：
1. `RegistrationAppletController.java` - 挂号Controller
2. `RegistrationServiceImpl.java` - 挂号Service实现
3. `RegistrationMapper.java` - 挂号Mapper（数据库操作）
4. `MessageController.java` - 消息Controller
5. `MessageServiceImpl.java` - 消息Service实现
6. `AppointmentReminderTask.java` - 提醒任务类

### 前端文件：
1. `payment.vue` - 支付页面
2. `messages.vue` - 消息中心页面
3. `registration.ts` - 挂号API定义

### 数据库表：
1. `registration_record` - 挂号记录表
2. `message_log` - 消息表
3. `patient` - 患者表
4. `hos_user` - 用户表
5. `doctor_schedule` - 排班表
6. `doctor` - 医生表
7. `department` - 科室表
8. `registration_type` - 挂号类型表

