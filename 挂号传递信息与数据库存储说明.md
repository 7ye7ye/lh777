# 挂号成功后传递给后端的信息及数据库存储说明

## 📤 前端传递给后端的信息

### **1. HTTP 请求信息**

**请求方式**：`POST /applet/registration/create`

**请求参数**：
- **URL参数**：
  - `patientId`：患者ID（从本地存储获取：`uni.getStorageSync('patientId')`）
  - `joinWaitingQueue`：是否加入候补队列（默认 `false`）

- **请求体（JSON格式）**：`RegistrationRecord` 对象

### **2. 请求体详细内容（前端构建）**

```javascript
{
  scheduleId: 123,           // 排班ID（从页面参数获取）
  patientId: 1,             // 患者ID（从本地存储获取，但后端会重新设置）
  doctorId: 16,             // 医生ID（从页面参数获取）
  typeId: 1,                // 号源类型ID（普通号/专家号，从页面参数获取）
  registrationNo: "20251120161001",  // 挂号单号（前端生成或后端生成）
  registerTime: "2025-11-20 16:10:01",  // 挂号时间（前端生成）
  status: 1,                // 状态：1-已预约
  priceOriginal: 50.00,     // 原价（从页面获取）
  actualPrice: 50.00,       // 实付价（从页面获取，后端会根据患者类型重新计算）
  isAdd: 0                  // 是否加号：0-正常号，1-加号
}
```

### **3. 前端代码位置**

**文件**：`校医院挂号系统/subpkg/hospital/payment.vue`

**关键代码**：
```javascript
const record = {
  scheduleId: scheduleId.value,      // 从页面参数获取
  patientId,                         // 从本地存储获取
  doctorId: doctorId.value,          // 从页面参数获取
  typeId: typeId.value,              // 从页面参数获取
  registrationNo: generateRegistrationNo(),  // 前端生成
  registerTime: formatLocalDateTime(new Date()),  // 前端生成
  status: 1,                         // 固定值：已预约
  priceOriginal: fee.value,          // 从页面显示的费用获取
  actualPrice: fee.value,            // 从页面显示的费用获取
  isAdd: 0                           // 固定值：正常号
}

await createRegistration(record, patientId, true)
```

---

## 💾 后端存储到数据库的信息

### **主要存储表：`registration_record`（挂号记录表）**

#### **存储的字段及来源**：

| 字段名 | 数据类型 | 说明 | 数据来源 |
|--------|---------|------|---------|
| `record_id` | bigint | 挂号记录ID（主键，自增） | **数据库自动生成** |
| `schedule_id` | bigint | 排班ID | **前端传递** |
| `patient_id` | bigint | 患者ID | **前端传递（URL参数）** |
| `doctor_id` | bigint | 医生ID | **后端从排班表查询获取**（`doctor_schedule.doctor_id`） |
| `type_id` | bigint | 号源类型ID | **前端传递** |
| `registration_no` | varchar(30) | 挂号单号 | **后端生成**（如果前端未传递）<br>格式：`YYYYMMDD + 科室ID + 序号` |
| `register_time` | datetime | 挂号时间 | **后端生成**（`LocalDateTime.now()`） |
| `status` | tinyint | 挂号状态 | **后端设置**：`1`（已预约） |
| `price_original` | decimal(10,2) | 原价 | **后端从挂号类型表查询**（`registration_type.price_original`） |
| `actual_price` | decimal(10,2) | 实付价 | **后端根据患者类型计算**<br>- 学生：`registration_type.student_price`<br>- 教职工：`registration_type.staff_price`<br>- 其他：`registration_type.price_original` |
| `is_add` | tinyint | 是否加号 | **前端传递**（默认 `0`：正常号） |
| `consult_room` | varchar(20) | 诊室号 | **NULL**（就诊前分配） |
| `visit_time` | datetime | 实际就诊时间 | **NULL**（就诊时填写） |
| `waiting_rank` | int | 候补排名 | **NULL**（仅候补状态有效） |
| `cancel_time` | datetime | 取消/退号时间 | **NULL**（取消时填写） |
| `cancel_reason` | varchar(200) | 取消/退号原因 | **NULL**（取消时填写） |
| `add_remark` | varchar(200) | 加号备注 | **NULL**（仅加号时填写） |

#### **SQL插入语句**：

```sql
INSERT INTO registration_record (
    schedule_id, patient_id, doctor_id, type_id, 
    registration_no, register_time, status, 
    price_original, actual_price, is_add
) VALUES (
    #{scheduleId},      -- 前端传递
    #{patientId},       -- 前端传递
    #{doctorId},        -- 后端从排班表查询
    #{typeId},          -- 前端传递
    #{registrationNo},  -- 后端生成
    #{registerTime},    -- 后端生成（当前时间）
    1,                  -- 固定值：已预约
    #{priceOriginal},   -- 后端从挂号类型表查询
    #{actualPrice},     -- 后端根据患者类型计算
    0                   -- 前端传递（正常号）
)
```

---

### **关联更新表：`doctor_schedule`（医生排班表）**

#### **更新的字段**：

| 字段名 | 更新内容 | 说明 |
|--------|---------|------|
| `used_quota` | `used_quota + 1` | 已用号源数 +1 |
| `update_time` | `NOW()` | 更新时间 |

#### **SQL更新语句**：

```sql
UPDATE doctor_schedule 
SET used_quota = #{usedQuota}, 
    update_time = NOW() 
WHERE schedule_id = #{scheduleId}
```

---

### **自动创建表：`message_log`（消息表）**

#### **存储的字段及来源**：

| 字段名 | 数据类型 | 说明 | 数据来源 |
|--------|---------|------|---------|
| `message_id` | int | 消息ID（主键，自增） | **数据库自动生成** |
| `user_id` | varchar(255) | 接收消息的用户ID | **后端从Token解析**（`hos_user.user_id`） |
| `appointment_id` | varchar(255) | 关联的预约ID | **后端设置**：`registration_record.record_id`（转为字符串） |
| `message_type` | varchar(50) | 消息类型 | **后端设置**：`"APPOINTMENT_SUCCESS"` |
| `title` | varchar(255) | 消息标题 | **后端设置**：`"预约挂号成功提醒"` |
| `content` | json | 消息内容（JSON格式） | **后端组装**，包含：<br>- `patient_card_no`：患者卡号<br>- `patient_name`：患者姓名<br>- `doctor_name`：医生姓名<br>- `department_name`：科室名称<br>- `appointment_time`：预约时间<br>- `hospital_remark`：医院备注 |
| `created_time` | datetime | 消息创建时间 | **后端生成**（`LocalDateTime.now()`） |
| `is_read` | tinyint | 是否已读 | **后端设置**：`0`（未读） |

#### **消息内容JSON示例**：

```json
{
  "patient_card_no": "P20250001",
  "patient_name": "张三",
  "doctor_name": "李医生",
  "department_name": "内科",
  "appointment_time": "2025-11-21 08:00-12:00",
  "hospital_remark": "请提前15分钟到达诊室候诊"
}
```

#### **SQL插入语句**：

```sql
INSERT INTO message_log (
    user_id, appointment_id, message_type, 
    title, content, created_time, is_read
) VALUES (
    '262',                    -- 从Token解析的用户ID
    '23',                     -- 挂号记录ID（record_id）
    'APPOINTMENT_SUCCESS',    -- 消息类型
    '预约挂号成功提醒',        -- 标题
    '{"patient_card_no":"P20250001",...}',  -- JSON内容
    '2025-11-20 16:10:01',    -- 当前时间
    0                         -- 未读
)
```

---

## 🔄 后端处理流程中的数据库操作

### **1. 查询操作（读取数据）**

#### **查询排班信息**：
```sql
SELECT * FROM doctor_schedule WHERE schedule_id = #{scheduleId}
```
- **目的**：获取医生ID、科室ID、已用号源数等
- **结果**：用于设置 `doctor_id` 和检查号源是否已满

#### **查询挂号类型**：
```sql
SELECT * FROM registration_type WHERE type_id = #{typeId}
```
- **目的**：获取原价、学生价、教职工价、每日号源数等
- **结果**：用于计算实际价格和检查号源是否已满

#### **查询患者信息**：
```sql
SELECT * FROM patient WHERE patient_id = #{patientId}
```
- **目的**：获取患者类型（学生/教职工）
- **结果**：用于计算实际价格（学生用学生价，教职工用教职工价）

#### **检查重复挂号**：
```sql
SELECT COUNT(*) FROM registration_record 
WHERE patient_id = #{patientId} 
AND schedule_id = #{scheduleId} 
AND status != 3
```
- **目的**：防止重复挂号
- **结果**：如果 > 0，返回错误

---

### **2. 插入操作（写入数据）**

#### **插入挂号记录**：
```sql
INSERT INTO registration_record (...) VALUES (...)
```
- **关键**：使用 `@Options(useGeneratedKeys = true)` 获取自动生成的 `record_id`

#### **插入消息记录**：
```sql
INSERT INTO message_log (...) VALUES (...)
```
- **时机**：挂号记录插入成功后立即执行

---

### **3. 更新操作（修改数据）**

#### **更新排班已用号源数**：
```sql
UPDATE doctor_schedule 
SET used_quota = used_quota + 1, update_time = NOW() 
WHERE schedule_id = #{scheduleId}
```

---

## 📊 数据流程图

```
前端传递数据
  ↓
{
  scheduleId: 123,
  typeId: 1,
  patientId: 1 (URL参数),
  registrationNo: "20251120161001" (可选),
  priceOriginal: 50.00 (可选),
  actualPrice: 50.00 (可选),
  isAdd: 0
}
  ↓
后端处理
  ↓
1. 查询 doctor_schedule → 获取 doctor_id
2. 查询 registration_type → 获取价格信息
3. 查询 patient → 获取患者类型
4. 检查重复挂号
5. 计算实际价格（根据患者类型）
6. 生成挂号单号（如果未传递）
7. 设置 register_time = 当前时间
8. 设置 status = 1
  ↓
插入 registration_record
  ↓
获取 record_id（自动生成）
  ↓
更新 doctor_schedule.used_quota
  ↓
查询挂号详情（多表联查）
  ↓
创建消息
  ↓
插入 message_log
```

---

## 🔑 关键点总结

### **1. 前端传递 vs 后端生成**

| 字段 | 前端传递 | 后端生成/查询 |
|------|---------|--------------|
| `schedule_id` | ✅ | |
| `type_id` | ✅ | |
| `patient_id` | ✅ | |
| `doctor_id` | | ✅ 从排班表查询 |
| `registration_no` | ⚠️ 可选 | ✅ 如果未传递则生成 |
| `register_time` | ⚠️ 可选 | ✅ 如果未传递则使用当前时间 |
| `price_original` | ⚠️ 可选 | ✅ 从挂号类型表查询 |
| `actual_price` | ⚠️ 可选 | ✅ 根据患者类型计算 |
| `status` | ⚠️ 可选 | ✅ 固定设置为 1 |
| `is_add` | ✅ | |

### **2. 数据验证和计算**

- **价格计算**：后端会根据患者类型重新计算 `actual_price`，前端传递的价格可能被覆盖
- **医生ID**：后端从排班表查询，确保数据一致性
- **挂号单号**：如果前端未传递，后端会自动生成
- **时间戳**：后端使用服务器时间，确保时间准确性

### **3. 关联数据存储**

- **挂号记录**：存储核心挂号信息
- **排班表更新**：更新已用号源数
- **消息记录**：自动创建成功消息，通知用户

---

## 📝 完整示例

### **前端请求示例**：

```http
POST /applet/registration/create?patientId=1&joinWaitingQueue=false
Content-Type: application/json

{
  "scheduleId": 123,
  "typeId": 1,
  "registrationNo": "20251120161001",
  "isAdd": 0
}
```

### **后端存储示例**：

**registration_record 表**：
```sql
INSERT INTO registration_record VALUES (
  23,                    -- record_id（自动生成）
  123,                   -- schedule_id（前端传递）
  1,                     -- patient_id（前端传递）
  16,                    -- doctor_id（后端查询）
  1,                     -- type_id（前端传递）
  '20251120161001',      -- registration_no（前端传递或后端生成）
  '2025-11-20 16:10:01', -- register_time（后端生成）
  1,                     -- status（后端设置）
  50.00,                 -- price_original（后端查询）
  2.50,                  -- actual_price（后端计算：学生价）
  NULL,                  -- waiting_rank
  NULL,                  -- consult_room
  NULL,                  -- visit_time
  NULL,                  -- cancel_time
  NULL,                  -- cancel_reason
  0,                     -- is_add（前端传递）
  NULL                   -- add_remark
);
```

**message_log 表**：
```sql
INSERT INTO message_log VALUES (
  1,                     -- message_id（自动生成）
  '262',                 -- user_id（后端从Token解析）
  '23',                  -- appointment_id（record_id）
  'APPOINTMENT_SUCCESS', -- message_type
  '预约挂号成功提醒',      -- title
  '{"patient_card_no":"P20250001","patient_name":"张三",...}',  -- content
  '2025-11-20 16:10:01', -- created_time
  0                      -- is_read
);
```

