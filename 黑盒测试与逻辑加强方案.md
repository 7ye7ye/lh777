# 黑盒测试与逻辑加强方案

本方案旨在对校医院挂号系统的核心功能模块进行全面的黑盒测试，并提出后端逻辑加强和前端体验优化建议，以确保系统的稳定性、安全性和用户友好性。

## 1. 登录注册模块

### 1.1 黑盒测试用例

| 测试类别 | 测试项 | 测试内容 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **通用** | 用户名/手机号 | 输入已注册的有效账号 | 正常登录 / 提示已注册 |
| | | 输入未注册的账号 | 提示用户不存在 / 可以注册 |
| | | 输入为空 | 提示“请输入用户名/手机号” |
| | | 输入超长字符串 (如 >50 chars) | 提示“用户名过长” |
| | | 输入含特殊字符 (`< > ' " /`) | 提示“用户名包含非法字符”或进行转义处理 |
| | 密码 | 输入正确的密码 | 正常登录/注册 |
| | | 输入错误的密码 | 提示“密码错误” |
| | | 输入为空 | 提示“请输入密码” |
| | | 输入超长密码 (如 >30 chars) | 提示“密码过长” |
| | | 输入过短密码 (如 <6 chars) | 提示“密码至少为6位” |
| | | 密码复杂度（数字、字母、特殊字符组合） | 根据设定的规则进行提示 |
| | 验证码 | 输入正确的验证码 | 正常通过 |
| | | 输入错误的验证码 | 提示“验证码错误” |
| | | 输入过期的验证码 | 提示“验证码已过期” |
| | | 频繁请求验证码 | 提示“请求过于频繁，请稍后再试” |
| **登录** | 角色登录 | 使用患者、医生、管理员账号登录 | 成功登录并跳转到对应角色的主页 |
| | 登录限制 | 密码错误次数过多 (如5次) | 账号被临时锁定，提示“密码错误次数过多，请15分钟后再试” |
| **注册** | 密码确认 | 两次输入的密码不一致 | 提示“两次输入的密码不一致” |
| | 账号唯一性 | 使用已存在的手机号/用户名注册 | 提示“该手机号/用户名已被注册” |

### 1.2 后端逻辑加强

-   **输入验证**: 对所有输入字段进行严格的格式和长度校验，特别是使用正则表达式校验手机号和用户名格式。必须进行防SQL注入和XSS攻击的过滤。
-   **安全性**: 
    -   密码必须经过哈希加盐（如 bcrypt）处理后才能存入数据库，绝不能明文存储。
    -   登录接口增加尝试次数限制，达到阈值后锁定账户一段时间。
    -   验证码应有时效性（如5分钟），使用一次后立即失效。
    -   登录成功后生成的Token应包含用户ID和角色信息，并设置合理的过期时间。
-   **业务逻辑**: 注册成功后，用户的初始状态应明确（例如，患者身份为“待认证”）。

### 1.3 前端优化建议

-   **实时校验**: 在用户输入时进行格式实时校验（如手机号格式、密码长度），并给出即时、友好的提示。
-   **明确提示**: 登录/注册失败时，给出具体错误信息（“密码错误”、“用户不存在”），而不是模糊的“操作失败”。
-   **加载状态**: 点击按钮后，显示加载动画（loading），并禁用按钮，防止用户重复提交请求。
-   **用户体验**: 提供“显示/隐藏密码”的切换功能。

---

## 2. 患者就诊卡 & 就诊人身份认证

### 2.1 黑盒测试用例

| 测试类别 | 测试项 | 测试内容 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **添加就诊人** | 姓名 | 输入合法的中文/英文姓名 | 添加成功 |
| | | 输入为空或包含数字/特殊字符 | 提示“姓名格式不正确” |
| | 身份证号 | 输入合法的18位身份证号 | 添加成功 |
| | | 输入为空、非18位或错误校验位 | 提示“身份证号格式不正确” |
| | | 在同一账户下添加已存在的身份证号 | 提示“该就诊人已存在” |
| | 手机号 | 输入合法的11位手机号 | 添加成功 |
| | | 输入非11位手机号 | 提示“手机号格式不正确” |
| **身份认证** | 上传照片 | 上传符合要求的身份证正反面照片 (JPG/PNG, <5MB) | 上传成功，等待审核 |
| | | 上传格式不符 (如 BMP, GIF) 或过大的图片 | 提示“请上传JPG或PNG格式的图片” / “图片大小不能超过5MB” |
| | | 不上传照片 | 无法提交审核 |
| | 提交审核 | 所有信息填写正确并上传照片 | 提交成功，状态变为“审核中” |

### 2.2 后端逻辑加强

-   **数据校验**: 
    -   后端必须有严格的身份证号格式和校验位算法验证，不能仅依赖前端。
    -   一个主账户下绑定的就诊人数量应设置上限（例如，最多5个）。
-   **业务逻辑**: 
    -   身份证号应作为全局唯一标识，防止不同主账户绑定同一个就诊人。
    -   患者提交身份认证后，其状态应变为“审核中”，在审核通过前，应限制其进行挂号等核心操作。
    -   记录认证状态的每次变更历史（提交、通过、驳回及操作人、时间）。
-   **文件处理**: 对上传的图片进行压缩和格式统一处理，并使用安全的方式存储（如OSS），防止未授权访问。

### 2.3 前端优化建议

-   **引导清晰**: 在页面上明确告知用户需要上传哪些材料，以及照片的格式、大小要求。
-   **交互优化**: 上传图片后提供预览、裁剪和删除重传功能。
-   **状态反馈**: 提交审核后，页面应清晰地展示“审核中”、“已通过”、“已驳回”等状态。若被驳回，必须醒目地显示驳回原因。

---

## 3. 医生个人主页 & 资料修改

### 3.1 黑盒测试用例

| 测试类别 | 测试项 | 测试内容 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **资料修改** |          |                          |                                        |
| | 个人简介 | 输入长文本 (如500 chars) | 修改成功，显示正常 |
| | | 输入包含HTML标签或脚本 | 标签被转义或过滤，脚本不执行 |
| | 头像上传 | 上传符合要求的图片 | 上传成功，头像更新 |
| | | 上传格式/大小不符的图片 | 给出相应提示 |
| | 提交修改 | 修改一项或多项信息 | 提交成功，信息进入待审核状态或直接更新 |

### 3.2 后端逻辑加强

-   **权限控制**: 严格校验接口权限，确保只有医生本人能修改自己的资料。
-   **数据校验**: 
    -   职称、科室等字段必须是系统中预设的有效值，防止通过抓包提交非法数据。
    -   对个人简介等富文本内容进行严格的XSS过滤。
-   **审核流程**: 
    -   明确哪些关键信息（如职称、科室、执业资格）的修改需要经过管理员审核。
    -   修改提交后，生成一条待审核记录。审核通过后才更新医生主页的公开信息。在审核期间，医生端应能看到待审核的新信息和审核状态。

### 3.3 前端优化建议

-   **数据加载**: 进入修改页面时，正确回填医生当前的资料。
-   **交互反馈**: 保存成功后给出明确的成功提示。如果需要审核，应告知用户“修改已提交，请等待管理员审核”。
-   **编辑体验**: 对于个人简介等长文本，使用体验更好的富文本编辑器。

---

## 4. 医生注册

### 4.1 黑盒测试用例

| 测试项 | 测试内容 | 预期结果 |
| :--- | :--- | :--- |
| **注册信息** | 姓名/身份证/手机号 | 输入合法、格式正确的信息 |
| | | 输入格式错误或已存在的信息 |

### 4.2 后端逻辑加强

-   **数据唯一性**: 医生的身份证号、手机号、执业医师编号都应是系统内的唯一标识。
-   **审核流程**: 医生注册后，账户状态必须是“待审核”，不能直接变为可用状态。只有管理员审核通过后，才能登录和使用系统。
-   **安全性**: 对上传的所有资质文件进行安全检查和存储。

### 4.3 前端优化建议

-   **流程引导**: 采用步骤条（Steps）组件，清晰地引导医生完成注册流程（填写基本信息 -> 上传资质 -> 提交审核）。
-   **状态跟踪**: 医生提交申请后，应能在登录页或特定查询页看到自己的审核状态（审核中、已通过、已驳回及原因）。

---

## 5. 管理员端功能模块

### 5.1 黑盒测试用例

| 测试类别 | 测试项 | 测试内容 | 预期结果 |
| :--- | :--- | :--- | :--- |
| **审核患者身份** | 列表展示 | 正确显示所有待审核的患者列表，信息完整 | 列表加载正常 |
| | 审核操作 | 对患者执行“通过”操作 | 该患者状态变为“已认证”，列表刷新 |
| | | 对患者执行“驳回”操作，并填写驳回原因 | 该患者状态变为“已驳回”，列表刷新，驳回原因被记录 |
| **审核医生资料变更** | 列表展示 | 正确显示所有待审核的医生资料变更项 | 列表加载正常 |
| | 审核操作 | 对变更项执行“通过”或“驳回”操作 | 对应医生的资料被更新或保持不变，审核记录消失 |
| **创建医生账户** | 账户创建 | 输入合法的医生姓名、手机号、初始密码、科室、职称 | 创建成功，医生列表出现新账户 |
| | | 输入已存在的手机号 | 提示“该手机号已被注册” |
| | | 输入信息不合法（如手机格式错误） | 提示格式错误 |

### 5.2 后端逻辑加强

-   **权限控制**: 所有管理员接口必须进行严格的权限校验，确保只有管理员角色的用户才能访问。
-   **原子操作**: 审核操作（通过/驳回）应是原子性的，即更新用户状态和写入审核记录应在同一个事务中完成。
-   **日志记录**: 对所有管理员的关键操作（审核、创建账户、修改信息等）进行详细的日志记录，包括操作人、操作时间、操作对象和IP地址，便于追溯。
-   **密码安全**: 创建医生账户时，后台生成的初始密码应是高强度随机的，并强制要求医生首次登录后立即修改密码。

### 5.3 前端优化建议

-   **信息展示**: 在审核列表页，清晰地展示待审核信息的关键内容（如患者姓名、身份证号，医生修改前后的信息对比），方便管理员快速决策。
-   **批量操作**: 如果业务需要，可以考虑提供批量审核功能。
-   **交互确认**: 对于“通过”、“驳回”、“创建”等重要操作，使用对话框（Modal）进行二次确认，防止误操作。
-   **搜索与筛选**: 提供按姓名、手机号等条件的搜索功能，以及按申请时间排序、筛选的功能，提高审核效率。
