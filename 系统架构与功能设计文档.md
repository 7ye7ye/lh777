# 校医院挂号系统 - 系统架构与功能设计文档

## 一、系统架构设计（三层架构）

本系统采用经典的B/S三层架构设计，分为**表示层（Presentation Layer）**、**业务逻辑层（Business Logic Layer）**和**数据访问层（Data Access Layer）**：

**表示层**包含Web管理端（基于Vue3+Vite+Ant Design Vue构建的SPA应用）和微信小程序端（基于uni-app开发的跨平台移动端），提供用户交互界面，通过RESTful API与后端通信，负责数据展示、用户输入收集和前端路由管理；**业务逻辑层**基于Spring Boot框架构建，采用模块化设计（hospital-module为核心业务模块、system-module为基础系统模块、airag-module为AI助手模块），使用Spring MVC的Controller层接收HTTP请求，Service层封装业务逻辑（如用户认证、挂号流程、排班管理、支付计算等），集成Shiro进行权限控制、Redis实现缓存和分布式会话、JWT实现无状态认证，支持WebSocket实现实时消息推送；**数据访问层**使用MyBatis-Plus作为ORM框架，通过Mapper接口访问MySQL数据库，包含10余张核心业务表（hos_user用户表、patient患者表、doctor医生表、department科室表、doctor_schedule排班表、registration_record挂号记录表、appointment预约表、message消息表等），支持事务管理和多数据源配置，确保数据一致性和系统可扩展性。整体架构遵循分层解耦、高内聚低耦合的设计原则，支持微服务化改造（已预留cloud模块），具备良好的可维护性和可扩展性。

---

## 二、系统功能结构设计（功能模块）

### 核心功能模块划分：

#### 1. **用户管理模块**
- **子功能**：用户注册、登录认证（支持验证码）、退出登录、密码管理、用户信息维护
- **用户角色**：患者（学生/教师/职工）、医生、管理员三种角色，基于`hos_user`表统一管理
- **权限控制**：Web端简化权限模型（登录即授权所有功能），小程序端按角色区分功能权限

#### 2. **患者管理模块**
- **就诊卡管理**：创建就诊卡、身份认证审核、患者信息完善（基本信息、病史信息、紧急联系人）
- **个人档案**：健康档案维护、过敏史管理、既往病史记录、体检数据录入
- **我的医生**：绑定常用医生、医患关系管理

#### 3. **科室管理模块**
- **科室架构**：两级科室体系（一级科室如"内科"、二级科室如"消化内科"）
- **科室信息**：科室介绍、位置导航、医生列表展示
- **科室查询**：支持按科室名称、位置、医生擅长领域检索

#### 4. **医生管理模块**
- **医生信息**：职称、擅长领域、简介、头像、出诊状态维护
- **医生审核**：医生信息修改审核流程（待审核/已通过/已驳回）
- **医生查询**：按科室、职称、擅长领域筛选医生

#### 5. **排班管理模块**
- **排班配置**：医生按日期、时段（上午/下午/晚上）设置出诊计划
- **号源管理**：设置号源总量、已用号源追踪、剩余号源展示
- **排班查询**：患者端查看医生排班、号源实时状态

#### 6. **挂号预约模块**
- **在线挂号**：选择科室→选择医生→选择时段→确认挂号→支付
- **挂号记录**：查询历史挂号记录、挂号状态追踪（已预约/已就诊/已退号/已取消）
- **退号管理**：患者主动退号、系统/医生取消挂号、退款处理
- **候补机制**：号源满时自动转入候补队列、有号源时按排名通知

#### 7. **就诊管理模块**
- **就诊流程**：签到→候诊→诊室分配→就诊→处方开具
- **患者队列**：医生端查看当日患者列表、患者详情查询
- **诊疗记录**：病情描述、诊断结果、处方记录、检查建议

#### 8. **消息通知模块**
- **站内消息**：系统通知、挂号提醒、就诊提醒、审核结果通知
- **实时推送**：基于WebSocket的实时消息推送（挂号成功、排队叫号等）
- **消息管理**：消息列表、已读/未读标记、消息删除

#### 9. **系统管理模块（后台）**
- **用户管理**：用户列表、账号状态管理（激活/禁用）、密码重置
- **角色权限**：角色定义、权限分配、菜单管理
- **数据字典**：挂号类型、排班类型、患者类型等字典维护
- **系统日志**：操作日志、登录日志、异常日志记录与查询
- **系统配置**：系统参数配置、公告管理

#### 10. **AI智能助手模块**
- **智能问诊**：症状描述→科室推荐→医生推荐
- **知识库问答**：常见病症查询、用药指导、就医流程咨询
- **文档解析**：病历文档上传与解析

#### 11. **统计分析模块**
- **就诊统计**：门诊量统计、科室就诊分布、医生工作量统计
- **挂号分析**：挂号渠道分析、退号率分析、高峰时段分析
- **数据大屏**：实时数据展示、可视化图表

---

## 三、系统架构图（文字描述）

```
┌─────────────────────────────────────────────────────────────┐
│                         表示层                               │
├──────────────────────┬──────────────────────────────────────┤
│   Web管理端          │      微信小程序端                     │
│ (Vue3 + Vite +       │   (uni-app + Vue3)                   │
│  Ant Design Vue)     │   患者端、医生端                      │
│  管理后台、医生端     │                                       │
└──────────────────────┴──────────────────────────────────────┘
                              ↕ HTTP/HTTPS (RESTful API + WebSocket)
┌─────────────────────────────────────────────────────────────┐
│                      业务逻辑层                              │
├─────────────────────────────────────────────────────────────┤
│  Spring Boot (多模块架构)                                    │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐ │
│  │ hospital-   │ system-     │ airag-      │ demo-       │ │
│  │ module      │ module      │ module      │ module      │ │
│  │ (核心业务)   │ (基础系统)   │ (AI助手)    │ (示例)      │ │
│  └─────────────┴─────────────┴─────────────┴─────────────┘ │
│  ┌─────────────────────────────────────────────────────────┐│
│  │ 安全框架: Shiro + JWT    缓存: Redis                    ││
│  │ 消息推送: WebSocket      任务调度: Quartz/XXL-Job       ││
│  │ 文件存储: OSS            监控: Actuator + Sentinel       ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
                              ↕ MyBatis-Plus ORM
┌─────────────────────────────────────────────────────────────┐
│                      数据访问层                              │
├─────────────────────────────────────────────────────────────┤
│  MySQL 数据库                                                │
│  核心表：hos_user, patient, doctor, department,             │
│         doctor_schedule, registration_record, appointment,  │
│         message, patient_visit 等                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 四、技术栈总结

### 前端技术栈
- **Web端**: Vue3 + Vite + TypeScript + Ant Design Vue + Pinia + Vue Router
- **小程序端**: uni-app + Vue3 + TypeScript + uni-ui

### 后端技术栈
- **核心框架**: Spring Boot 3.x + Spring MVC
- **ORM框架**: MyBatis-Plus
- **安全框架**: Apache Shiro + JWT
- **缓存**: Redis
- **数据库**: MySQL 8.0+
- **消息推送**: WebSocket
- **任务调度**: Quartz/XXL-Job
- **文档**: Swagger/Knife4j
- **云服务**: 支持微服务化（Nacos、Gateway、Sentinel）

### 开发工具
- **构建工具**: Maven (后端) + npm/pnpm (前端)
- **版本控制**: Git
- **API测试**: Postman/Swagger UI

---

## 五、系统特色与亮点

1. **前后端分离架构**：Web端和小程序端独立开发，通过统一API接口交互
2. **模块化设计**：hospital-module为独立业务模块，可插拔，易扩展
3. **多端支持**：Web管理端（管理员/医生）+ 微信小程序（患者/医生双端）
4. **权限简化**：Web端简化权限模型，登录即可访问所有功能，便于校园内部使用
5. **实时通信**：基于WebSocket的实时消息推送，提升用户体验
6. **AI赋能**：集成AI助手模块，提供智能导诊和知识问答
7. **安全可靠**：敏感数据加密存储、JWT无状态认证、操作日志审计
8. **高性能**：Redis缓存、数据库索引优化、分页查询优化

---

## 六、数据库设计要点

### 核心表关系
- `hos_user` (用户表) ← 1:1 → `patient` (患者表)
- `hos_user` (用户表) ← 1:1 → `doctor` (医生表)
- `doctor` (医生表) ← N:1 → `department` (科室表)
- `doctor_schedule` (排班表) ← N:1 → `doctor` (医生表)
- `registration_record` (挂号记录) ← N:1 → `patient`, `doctor`, `doctor_schedule`
- `appointment` (预约表) ← N:1 → `patient`, `doctor`
- `message` (消息表) ← N:1 → `hos_user`

### 数据安全
- 密码加密：MD5 + SALT
- 敏感字段：身份证号、手机号加密存储
- 审计字段：create_time、update_time记录数据变更

---

**编写日期**: 2025年10月24日  
**系统版本**: v1.0  
**技术架构**: JeecgBoot 3.8.1 + Vue3 + uni-app

