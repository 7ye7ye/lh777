# 页面更新说明

## 旧页面 → 新页面对比

### 1. 我的就诊人页面 (`mypatient.vue`)

#### 旧版本特点
- ❌ 显示完整的姓名、性别、年龄
- ❌ 显示完整的身份证号和手机号
- ❌ 每个就诊人有"编辑"和"删除"按钮
- ❌ 简单的列表样式

#### 新版本特点
- ✅ 医院名称头部："中国医科大学附属盛京医院"
- ✅ 电子就诊卡标识（红色标签）
- ✅ 渐变色头像（默认显示姓名首字母）
- ✅ 姓名和身份证号脱敏显示（420***********961）
- ✅ 每个就诊人卡片右侧显示二维码
- ✅ 底部统计："已添加X位就诊人，还可以添加X位"
- ✅ 统一的"添加就诊人"按钮
- ✅ "解绑电子就诊卡"链接

#### 核心改进
```vue
<!-- 新版本卡片结构 -->
<view class="patient-card">
  <view class="patient-info">
    <view class="avatar-container">
      <!-- 渐变色圆形头像 -->
    </view>
    <view class="info-content">
      <text class="patient-name">曹玥</text>
      <text class="patient-id">420***********961</text>
    </view>
  </view>
  <view class="qrcode-wrapper">
    <!-- 蓝色二维码 -->
  </view>
</view>
```

### 2. 我的医生页面 (`mydoctor.vue`)

#### 旧版本特点
- ❌ 显示医生列表（李医生、王医生、张医生）
- ❌ 搜索框
- ❌ 医生信息：姓名、职称、科室、简介、标签
- ❌ "联系"按钮
- ❌ "刷新医生列表"按钮

#### 新版本特点
- ✅ 显示空状态页面
- ✅ 精美的插图动画
- ✅ "暂无数据"文字提示
- ✅ 符合用户当前无医生数据的实际情况

#### 核心改进
```vue
<!-- 新版本空状态 -->
<view class="empty-container">
  <view class="empty-illustration">
    <!-- 文档图标 + 搜索图标 -->
    <!-- 装饰性圆点和圆圈 -->
    <!-- 呼吸、漂浮、闪烁动画 -->
  </view>
  <text class="empty-text">暂无数据</text>
</view>
```

### 3. 添加就诊人页面

#### 变化
- 旧版本：弹窗形式，字段较少
- 新版本：完整页面，详细表单（已在 `create-card.vue` 中实现）

#### 表单字段对比

**旧版本字段：**
- 姓名
- 证件类型
- 证件号码
- 性别
- 出生日期
- 手机号

**新版本字段：**
- 就诊人信息
  - 姓名
  - 证件类型
  - 证件号码
  - 性别
  - 出生日期
  - 民族
  - 所在地区
  - 详细住址
  - 电话号码
  - 短信验证码
  - 国籍
- 健康档案（选填，跳转到健康档案页面）

### 4. 健康档案页面（新增）

这是一个完全新增的功能页面，包含：

#### 页面特点
- ✅ 顶部健康主题插图
- ✅ "关爱健康·感恩生活"标语
- ✅ 渐变背景（蓝色到灰色）
- ✅ 白色卡片设计

#### 表单内容
**基本信息：**
- 身高(cm)
- 体重(kg)
- 血型
- 婚姻状况
- 生育情况

**健康信息：**
- 现病史
- 既往史
- 家族史
- 过敏史

#### 保存按钮
- 橙色渐变按钮
- 圆角设计
- 带阴影效果

## 技术实现要点

### 1. 数据脱敏
```javascript
// 身份证号脱敏
const maskIdNumber = (idCard) => {
  if (!idCard) return '未填写'
  if (idCard.length < 18) return idCard
  return idCard.substring(0, 3) + '**********' + idCard.substring(14)
}
// 结果：420**********961
```

### 2. 二维码生成
```vue
<uqrcode 
  :canvas-id="'patient-qr-' + patient.patientId"
  :value="patient.outpatientNumber" 
  :size="120"
  :margin="5"
  background-color="#FFFFFF"
  foreground-color="#3a9cff"
  file-type="png"
></uqrcode>
```

### 3. 渐变头像
```css
.default-avatar {
  width: 96rpx;
  height: 96rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  display: flex;
  align-items: center;
  justify-content: center;
}
```

### 4. 空状态动画
```css
@keyframes pulse {
  0%, 100% {
    transform: scale(1);
    opacity: 0.6;
  }
  50% {
    transform: scale(1.05);
    opacity: 0.8;
  }
}

@keyframes float {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-20rpx);
  }
}
```

## API 接口说明

### 已实现的接口（前端）

#### 就诊人相关
- `getPatientList(data: { userId: number })` - 获取就诊人列表
- `addPatient(data: CreateCardRequest)` - 添加就诊人
- `updatePatient(data: UpdateCardRequest)` - 更新就诊人
- `deletePatient(data: { userId: number; patientId: number })` - 删除就诊人

#### 健康档案相关
- `getHealthProfile(data: { patientId: number })` - 获取健康档案
- `updateHealthProfile(data: HealthProfileRequest)` - 更新健康档案

### 需要后端实现的接口

#### 1. 获取就诊人列表
```
POST /patient/list
Request:
{
  "userId": 123
}

Response:
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "patientId": 1,
      "patientName": "曹玥",
      "idCard": "420***********961",
      "phone": "15*****467",
      "gender": "女",
      "birthDate": "1995-01-01",
      "outpatientNumber": "M017080045",
      "avatar": "" // 可选
    }
  ]
}
```

#### 2. 添加就诊人
```
POST /patient/create
Request:
{
  "userId": 123,
  "patientName": "张三",
  "idType": "身份证",
  "idCard": "110101199001011234",
  "gender": "男",
  "birthDate": "1990-01-01",
  "nation": "汉族",
  "nationality": "中国",
  "region": "北京市海淀区",
  "detailedAddress": "北京交通大学",
  "phone": "13800138000"
}

Response:
{
  "code": 200,
  "message": "添加成功",
  "data": {
    "patientId": 2,
    "outpatientNumber": "M017080046" // 系统自动生成
  }
}
```

#### 3. 获取健康档案
```
POST /patient/health/get
Request:
{
  "patientId": 1
}

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "patientId": 1,
    "height": "170",
    "weight": "60",
    "bloodType": "A型",
    "maritalStatus": "未婚",
    "fertilityStatus": "未育",
    "currentIllness": "",
    "pastHistory": "",
    "familyHistory": "",
    "allergyHistory": "无"
  }
}
```

#### 4. 更新健康档案
```
POST /patient/health/update
Request:
{
  "patientId": 1,
  "height": "170",
  "weight": "60",
  "bloodType": "A型",
  "maritalStatus": "未婚",
  "fertilityStatus": "未育",
  "currentIllness": "",
  "pastHistory": "",
  "familyHistory": "",
  "allergyHistory": "无"
}

Response:
{
  "code": 200,
  "message": "保存成功"
}
```

## 样式规范

### 颜色系统
- 主色：`#3a9cff` （蓝色）
- 辅助色：`#52c41a`（绿色）
- 警告色：`#ff4d4f`（红色）
- 健康色：`#ffa726`（橙色）
- 渐变：`linear-gradient(135deg, #667eea 0%, #764ba2 100%)`

### 尺寸规范
- 圆角：`16rpx`
- 阴影：`0 4rpx 16rpx rgba(0, 0, 0, 0.06-0.08)`
- 卡片内边距：`32rpx`
- 间距：`24rpx`、`32rpx`、`40rpx`

### 字体规范
- 大标题：`44rpx`
- 标题：`32-36rpx`
- 正文：`28-30rpx`
- 辅助文字：`24-26rpx`
- 小文字：`22rpx`

## 页面路由

### 页面配置（pages.json）
```json
{
  "path": "personal/mypatient",
  "style": {
    "navigationBarTitleText": "我的就诊人"
  }
},
{
  "path": "personal/mydoctor",
  "style": {
    "navigationBarTitleText": "我的医生"
  }
},
{
  "path": "personal/create-card",
  "style": {
    "navigationBarTitleText": "添加就诊人"
  }
},
{
  "path": "personal/health-profile",
  "style": {
    "navigationBarTitleText": "健康档案"
  }
}
```

### 页面跳转
```javascript
// 跳转到添加就诊人
uni.navigateTo({ 
  url: '/subpkg/profile/personal/create-card' 
})

// 跳转到健康档案
uni.navigateTo({ 
  url: `/subpkg/profile/personal/health-profile?patientId=${patientId}` 
})

// 查看就诊卡详情
uni.navigateTo({ 
  url: `/subpkg/profile/personal/mycard?patientId=${patientId}` 
})
```

## 更新总结

### 已完成的工作 ✅
1. ✅ 更新 `mypatient.vue` - 新样式的就诊人列表
2. ✅ 更新 `mydoctor.vue` - 空状态页面
3. ✅ 实现 `health-profile.vue` - 健康档案页面
4. ✅ 实现 `create-card.vue` - 添加就诊人页面
5. ✅ 更新 `mycard.vue` - 就诊卡详情（带二维码、条形码）
6. ✅ 更新 `modify-info.vue` - 修改就诊人信息
7. ✅ 定义所有 API 接口
8. ✅ 实现数据脱敏功能
9. ✅ 实现二维码生成功能
10. ✅ 实现页面刷新机制（onShow）
11. ✅ 在 pages.json 中注册所有页面

### 需要后端配合的工作
1. 实现就诊人相关 API
2. 实现健康档案相关 API
3. 生成门诊号逻辑
4. 数据库表设计和实现

### 测试建议
1. 测试添加就诊人流程
2. 测试健康档案录入
3. 测试数据脱敏显示
4. 测试二维码生成和显示
5. 测试页面刷新机制
6. 测试空状态显示

## 注意事项

1. **门诊号生成规则**
   - 建议格式：M + 年月日 + 流水号
   - 例如：M017080045

2. **数据脱敏**
   - 身份证号：显示前3位和后4位
   - 手机号：显示前3位和后4位

3. **二维码内容**
   - 使用门诊号作为二维码内容
   - 如果没有门诊号，使用 patientId

4. **健康档案图片**
   - 需要准备一张健康主题的插图
   - 建议尺寸：600rpx × 400rpx
   - 存放路径：`static/images/health-bg.png`

5. **最大就诊人数量**
   - 当前设置为 9 位
   - 可根据实际需求调整

## 开发建议

### 前端开发
- 所有页面已完成，可直接使用
- API 接口已定义，等待后端实现
- 样式严格遵循设计图

### 后端开发
- 参考"API 接口说明"章节实现接口
- 注意数据验证和错误处理
- 门诊号需要全局唯一

### 联调测试
1. 先实现就诊人列表和添加功能
2. 再实现健康档案功能
3. 最后测试完整流程

---

**更新完成时间：** 2025年10月31日  
**更新人：** AI Assistant  
**版本：** v2.0

